import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const SteaksScreen = () => {
  return (
    <SafeAreaView>
      <View className=''>
        <Text>This login page</Text>
      </View>
    </SafeAreaView>
  );
};

export default SteaksScreen;
